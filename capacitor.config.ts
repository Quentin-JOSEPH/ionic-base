import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wwd.com',
  appName: 'wwd',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
